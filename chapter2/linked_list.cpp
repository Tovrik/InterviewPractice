#include <iostream>
#include "Node.h"
using namespace std;

class LinkedList {
    private:
        Node<T> *head;
        head = new Node(NULL, 0);
    public:
        Node<T>* front();
        Node<T>* back();
};

Node<T>* LinkedList::front() {
    this.
}



int main() {
    LinkedList list;
    list = new LinkedList();
}
